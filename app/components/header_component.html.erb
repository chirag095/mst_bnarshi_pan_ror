<style type="text/css">
   #onebitesmenuleft a {
    text-decoration: none;
    padding: 10px;
    display: inline-block;
    position: relative;
}

/* Dropdown container */
.dropdown {
    position: relative;
    display: inline-block;
}

/* Dropdown menu */
.dropdown-menu {
    display: none;
    position: absolute;
    top: 100%;
    left: 0;
    background-color: #000000;
    padding: 10px;
    border: 1px solid #ddd;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    z-index: 10;
}

/* Dropdown menu items */
.dropdown-menu a {
    display: block;
    padding: 8px 12px;
    white-space: nowrap;
}

/* Show the dropdown menu on hover */
.dropdown:hover .dropdown-menu {
    display: block;
}

/* Styling for hover effect */
.dropdown-menu a:hover {
    background-color: #f0f0f0;
}
#toponebitelogobox {
    display: flex;
    justify-content: center;  /* Centers logo horizontally */
    align-items: center;  /* Centers logo vertically */
    height: 80px;  /* Adjust based on header height */
    background: black; /* Optional: Background color */
    padding: 10px;
}
</style>
<div id="toponebitelogo">
   <div id="toponebitelogoul">
      <div id="onebitesmenuleft">
         <a href="<%= root_path%>">Home</a>
         <a href="<%= about_path%>">About us</a>
         <div class="dropdown">
            <a href="<%= events_path %>">Events</a>
            <div class="dropdown-menu">
               <% @events.each do |event|%>
               <a href="<%=event_path(event)%>"><%=event.title%></a>
               <%end%>
            </div>
         </div>
         <a href="<%=gallery_path %>">Gallery</a>
        
         
         <div class="dropdown">
            <a href="#">Franchise Model</a>
            <div class="dropdown-menu">
               <% @pan_outlates.each do |outlate|%>
               <a href="<%=model_path(outlate)%>"><%=outlate.title%></a>
               <%end%>
            </div>
         </div>
      </div>
       <div id="toponebitelogobox">
      <img src="/assets/slider2.webp " style="width:170px; height:60px;">
   </div>
      <div id="onebitesmenulright">
         <a href="<%= award_path%>">Franchise Awards</a>
         <a href="<%=contact_us_path%>">Contact Us</a>       
         <a href="#">Franchise Enquiry</a>
         <a href="<%= blogs_path%>">News / Blogs</a>
      </div>
   </div>
</div>
